<h1 class="page-header">User</h1>
<div class="row">
	<div class="col-xs-12">
		<table class="table table-striped">
		    <tbody>
		        <tr>
		          	<td>Name: <%= @user.name if @user.name %></td>
    				<td>Email: <%= @user.email if @user.email %></td>
		        </tr>
		    </tbody>
		</table>
		<br>
		<%= link_to 'Back', :back %>
	</div>
</div>